import { ConfigTemplateParams } from './index.js';
/**
 * Generates the root-level compose.yml file with all containerized services
 */
export declare const generateCompose: (params: ConfigTemplateParams) => string;
